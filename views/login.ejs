<%-include('partials/nav')%>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="bg-white rounded-lg shadow-lg p-6 md:p-8">
      <form action="/login" method="POST"  class="space-y-6">
          
          
          <!-- Your Information Section -->
          <div >
              <h2 class="text-3xl font-bold text-gray-800 mb-4  text-center">
                 Sign-In 
              </h2>
              <div class="space-y-4">
                  

                  <!-- Email -->
                  <div>
                      <label class="block text-gray-700 font-semibold mb-2 ">
                          Email Address <span class="text-red-500">*</span>
                      </label>
                      <input 
                          type="email" 
                          name="email"
                          required
                         
                          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                  </div>

                  <!-- Password -->
                  <div>
                      <label class="block text-gray-700 font-semibold mb-2">
                          Password <span class="text-red-500">*</span>
                      </label>
                      <div class="flex text-center justify-center gap-3">
                      <input 
                          type="password" 
                          name="password"
                          required
                          id="pass"
                          class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
                          <button id="btn1" class="border rounded-lg p-2 font-semibold border-gray-300" type="button" onclick="toggleInputType('pass','btn1')" >Show</button></div>
                  </div>

                  
              </div>
             </div>     
            <div>
              <p>Don't have an account? <a href="/signup" class="text-blue-600">Signup </a></p>
            </div>

          <!-- Submit Button -->
          <div class="flex flex-col sm:flex-row gap-4 pt-4">
              <button 
                  type="submit"
                  class="flex-1 bg-indigo-600 text-white px-8 py-4 rounded-lg hover:bg-indigo-700 transition font-semibold text-lg shadow-lg hover:shadow-xl">
                  <i class="fas fa-paper-plane mr-2"></i>
                  Submit 
              </button>
              <button 
                  type="reset"
                  class="bg-gray-200 text-gray-700 px-8 py-4 rounded-lg hover:bg-gray-300 transition font-semibold">
                  <i class="fas fa-redo mr-2"></i>
                  Clear Form
              </button>
          </div>
      </form>
  </div>
</div>
<script>
    function toggleInputType(inputId,btnid) {
  const input = document.getElementById(inputId);
  const btn = document.getElementById(btnid)
  if (input.type === "password") {
    input.type = "text";
    btn.textContent = "Hide";
  } else {
    input.type = "password";
    btn.textContent = 'Show';
  }
}
</script>

<%- include('partials/footer')%>